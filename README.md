# PDP Generator

Система управления ассессментами и генерации индивидуальных планов развития (ИПР) для сотрудников.

## Возможности

- **Управление ассессментами** -- создание, назначение участников, смена статусов (запланирован, в процессе, завершён, отменён)
- **Проведение ассессментов** -- загрузка результатов из Excel или ввод оценок вручную
- **Генерация ИПР** -- автоматическое создание Word-документа с вопросами и заданиями по слабым темам
- **Хранение ИПР** -- сгенерированные файлы сохраняются на сервере и доступны для повторного скачивания
- **Дашборд** -- личная страница с ассессментами и планами развития
- **Управление пользователями** -- создание пользователей, назначение ролей

## Роли

| Роль | Возможности |
|------|-------------|
| **Пользователь** (USER) | Видит свои ассессменты и ИПР, редактирует профиль |
| **Асессор** (ASSESSOR) | Всё что может пользователь + создаёт ассессменты, проводит их, управляет пользователями |

## Требования

- Node.js 18+
- npm

## Установка и запуск

### 1. Установить зависимости

```bash
npm install
```

### 2. Настроить переменные окружения

Создайте файл `.env.local` в корне проекта:

```env
AUTH_SECRET=ваш-секретный-ключ-для-jwt
NEXTAUTH_URL=http://localhost:3000

# Google OAuth (https://console.cloud.google.com/ → APIs & Services → Credentials)
AUTH_GOOGLE_ID=ваш-client-id.apps.googleusercontent.com
AUTH_GOOGLE_SECRET=ваш-client-secret

# Разрешённые домены электронной почты (через запятую). По умолчанию: innowise.com
ALLOWED_EMAIL_DOMAIN=innowise.com
```

> Для `AUTH_SECRET` используйте любую случайную строку (`openssl rand -base64 32`). В продакшене замените на надёжный ключ.
>
> В Google Cloud Console в настройках OAuth-клиента добавьте разрешённый redirect URI: `http://localhost:3000/api/auth/callback/google` (и соответствующий URL продакшена).

### 3. Создать базу данных

```bash
npx prisma db push
```

Это создаст файл `data/pdp.db` (SQLite) со всеми таблицами.

### 4. Создать первого администратора

Пользователи создаются автоматически при первом входе через Google (с ролью `USER`). Чтобы назначить администратора, выполните одно из:

- Запустите `npx tsx prisma/seed.ts` — создаст тестовых пользователей, включая `admin@innowise.com` с ролью `ADMIN`.
- Либо откройте `npx prisma studio` и вручную измените роль нужного пользователя на `ADMIN`.

### 5. Запустить приложение

```bash
npm run dev
```

Приложение будет доступно по адресу [http://localhost:3000](http://localhost:3000).

### 6. Сборка для продакшена

```bash
npm run build
npm start
```

## Структура проекта

```
├── data/                       # Данные приложения
│   ├── mappings/               # Маппинг категорий к темам ИПР
│   │   ├── jun.json            # Маппинг для Junior
│   │   ├── mid.json            # Маппинг для Middle
│   │   ├── aliases.json        # Алиасы названий категорий
│   │   └── skip-names.json     # Исключаемые категории
│   ├── pdp-topics.md           # Вопросы и задания по темам
│   ├── template.docx           # Шаблон Word-документа
│   ├── pdps/                   # Сгенерированные ИПР файлы
│   └── pdp.db                  # База данных SQLite
├── prisma/
│   ├── schema.prisma           # Схема базы данных
│   └── seed.ts                 # Скрипт создания администратора
├── src/
│   ├── app/
│   │   ├── (app)/              # Страницы с аутентификацией
│   │   │   ├── dashboard/      # Дашборд пользователя
│   │   │   ├── assessments/    # Управление ассессментами
│   │   │   ├── generate/       # Генератор ИПР
│   │   │   ├── users/          # Управление пользователями
│   │   │   └── profile/        # Профиль пользователя
│   │   ├── api/                # API маршруты
│   │   │   ├── assessments/    # CRUD ассессментов
│   │   │   ├── users/          # Управление пользователями
│   │   │   ├── pdps/           # Записи ИПР и скачивание
│   │   │   ├── generate/       # Генерация Word-документа
│   │   │   ├── parse-excel/    # Парсинг Excel ассессмента
│   │   │   └── data/           # Метаданные (темы, маппинги)
│   │   └── login/              # Страница входа
│   ├── components/             # UI компоненты
│   │   ├── ui/                 # shadcn/ui компоненты
│   │   └── app-sidebar.tsx     # Боковая навигация
│   └── lib/                    # Утилиты
│       ├── auth.ts             # Конфигурация NextAuth
│       ├── prisma.ts           # Prisma клиент
│       ├── data-loader.ts      # Загрузка файлов данных
│       └── types.ts            # TypeScript типы
└── .env.local                  # Переменные окружения (не в git)
```

## База данных

Приложение использует SQLite через Prisma ORM. Файл БД: `data/pdp.db`.

### Модели

- **User** -- пользователи (имя, email, роль, грейд, проект, руководитель). Создаются автоматически при первом входе через Google.
- **Assessment** -- ассессменты (название, статус, грейд, даты)
- **AssessmentParticipant** -- участники ассессмента (оцениваемый / асессор)
- **AssessmentResult** -- результаты (категория, оценка, комментарий)
- **Pdp** -- записи сгенерированных ИПР (файл, статус, привязка к ассессменту)

### Полезные команды

```bash
# Сбросить БД и создать заново
npx prisma db push --force-reset
npx tsx prisma/seed.ts

# Открыть Prisma Studio (визуальный редактор БД)
npx prisma studio

# Сгенерировать Prisma Client после изменения схемы
npx prisma generate
```

## Стек технологий

- **Next.js 14** (App Router)
- **React 18**
- **TypeScript**
- **Prisma** + SQLite
- **NextAuth v5** (Auth.js)
- **shadcn/ui** + Tailwind CSS
- **xlsx** -- парсинг Excel файлов
- **jszip** -- генерация Word документов
